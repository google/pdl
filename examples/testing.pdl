little_endian_packets

// TODO(jmes): implement _size_ field to get encode/decode working for this case

packet ScalarParent {
    a: 8,
    _size_(_payload_): 8,
    _payload_
}

packet ScalarChild_A : ScalarParent (a = 0) {
    b: 8,
}